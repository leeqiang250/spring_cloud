server:
  port: 8762
spring:
  profiles:
    active: dev
  application:
    name: service-manager
  cloud:
    config:
      discovery:
        service-id: service-config
        enabled: true
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update